import { FinderDropdownDirective } from './finder-dropdown.directive';

describe('FinderDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new FinderDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
