import { FinderPrimaryButtonDirective } from './finder-primary-button.directive';

describe('FinderPrimaryButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FinderPrimaryButtonDirective();
    expect(directive).toBeTruthy();
  });
});
