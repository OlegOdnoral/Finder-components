import { FinderPrimaryDirective } from './finder-primary.directive';

describe('FinderPrimaryDirective', () => {
  it('should create an instance', () => {
    const directive = new FinderPrimaryDirective();
    expect(directive).toBeTruthy();
  });
});
